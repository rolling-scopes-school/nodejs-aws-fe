const endpoint = 'https://9441ep3x1i.execute-api.eu-west-1.amazonaws.com/dev'

const API_PATHS = {
  product: endpoint,
  order: endpoint,
  import: endpoint,
  bff: endpoint
};

export default API_PATHS;
